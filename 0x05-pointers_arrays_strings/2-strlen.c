#include "main.h"
#include <string.h>
/**
 * _strlen - determines length of a string
 * @s: input number
 * Return: length of string
 */
int _strlen(char *s)
{
	int coun;

	coun = strlen(s);
	return (coun);
}
