#include "main.h"
#include <string.h>
/**
 * _strlen_recursion - returns length of a string
 * @s: input
 * Return: length
 */
int _strlen_recursion(char *s)
{
	int a;

	a = strlen(s);
	return (a);
}
